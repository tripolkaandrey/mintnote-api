springdoc:
  swagger-ui:
    enabled: false
  api-docs:
    enabled: false
spring:
  cloud:
    gcp:
      firestore:
        emulator:
          enabled: true
        host-port: 0.0.0.0:9000
service-account:
  translation-api-user: ${sm://translate-api-user-service-account}
  firebase-auth-viewer: ${sm://firebase-auth-viewer-service-account}